---
- name: Deploy LocalAI evaluation runtime
  hosts: all
  become: true

  tasks:
    - name: Install LocalAI evaluation runtime
      ansible.builtin.import_role:
        name: localai
      when: localai_enabled
