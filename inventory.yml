all:
  hosts:
    lab:
      ansible_host: "{{ lookup('env', 'LAB_HOSTNAME') | default('localhost', true) }}"
      ansible_user: "{{ lookup('env', 'LAB_USER') | default('admin', true) }}"
      ansible_ssh_private_key_file: ./ssh/lab
      ansible_python_interpreter: /usr/bin/python3
